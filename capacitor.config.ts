import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'examen-1er-parcial',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
